-- LEGADO (compatibilidade): províncias/cidades de Moçambique já estão no database/schema.sql.
-- Para instalação nova, importe apenas database/schema.sql.
-- Este ficheiro permanece apenas para compatibilidade com rotinas antigas.
SELECT 'mozambique_locations.sql legacy noop: use database/schema.sql' AS info;
