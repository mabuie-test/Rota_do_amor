-- LEGADO (compatibilidade): seeds estruturais foram centralizados em database/schema.sql.
-- Para instalação nova, importe apenas database/schema.sql.
-- Este ficheiro foi mantido apenas para evitar falhas em pipelines antigas.
SELECT 'seed.sql legacy noop: use database/schema.sql' AS info;
